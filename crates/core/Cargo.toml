[package]
name = "raspberry-core"
description = "Shared system-access core: stats, processes, files. Used by Hub (local) and Agent (remote)."
version.workspace = true
edition.workspace = true
authors.workspace = true

[lib]
name = "raspberry_core"

[dependencies]
sysinfo = "0.33"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
netdev = "0.30"
rusqlite = { version = "0.32", features = ["bundled"] }
